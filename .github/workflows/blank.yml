name: 24-7 Keep Alive
on:
  schedule:
    - cron: '*/5 * * * *' # Runs every 5 minutes
  workflow_dispatch:      # Allows you to start it manually

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping My App
        run: curl -s https://requirements-txt-5-jx0j.onrender.com
